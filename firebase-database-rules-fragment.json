{
  "orders": {
    "$orderId": {
      ".read": "auth != null && data.child('userId').val() === auth.uid",
      ".write": "auth != null && ((!data.exists() && newData.child('userId').val() === auth.uid && newData.child('paymentMethod').val() === 'cod' && newData.child('paymentStatus').val() === 'pending') || (data.exists() && data.child('userId').val() === auth.uid && data.child('paymentStatus').val() === newData.child('paymentStatus').val() && data.child('cashfreeOrderStatus').val() === newData.child('cashfreeOrderStatus').val() && data.child('paidAt').val() === newData.child('paidAt').val() && data.child('paymentSessionId').val() === newData.child('paymentSessionId').val() && data.child('paymentMethod').val() === newData.child('paymentMethod').val() && data.child('finalAmount').val() === newData.child('finalAmount').val()))"
    }
  },
  "userOrders": {
    "$uid": {
      ".read": "auth != null && auth.uid === $uid",
      "$orderId": {
        ".write": "auth != null && auth.uid === $uid",
        ".validate": "newData.isBoolean()"
      }
    }
  },
  "outletOrders": {
    "$outletId": {
      ".read": "auth != null",
      "$orderId": {
        ".write": "auth != null"
      }
    }
  },
  "cashfreeWebhooks": {
    ".read": false,
    ".write": false
  }
}
