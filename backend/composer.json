{
  "name": "brisko/auth-api",
  "description": "Brisko Pizza PHP Auth API (OTP + Firebase custom tokens). No MySQL.",
  "require": {
    "php": ">=8.0"
  },
  "autoload": {
    "classmap": ["lib/"]
  }
}
